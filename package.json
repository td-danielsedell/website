{
  "name": "totaldigital-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "verify": "node verify/diff-pages.mjs",
    "verify:self-test": "node verify/diff-pages.mjs --self-test && node verify/assert-invariants.mjs --self-test && node verify/assert-base.mjs --self-test && node verify/assert-translations.mjs --self-test",
    "verify:invariants": "node verify/assert-invariants.mjs",
    "verify:baseline": "node verify/diff-pages.mjs --candidate verify/baseline && node verify/assert-invariants.mjs verify/baseline",
    "verify:serve": "node verify/serve.mjs",
    "build:preview": "astro build --base=/website/",
    "verify:base": "node verify/assert-base.mjs",
    "format": "prettier --write \"src/**/*.{astro,ts}\"",
    "format:check": "prettier --check \"src/**/*.{astro,ts}\"",
    "verify:translations": "node verify/assert-translations.mjs"
  },
  "devDependencies": {
    "astro": "7.3.1",
    "parse5": "8.0.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^1.0.0"
  }
}
