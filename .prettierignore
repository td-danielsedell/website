# verify/baseline/ is a byte-exact snapshot of the shipped pages and is the
# ground truth the build is compared against. Formatting it would silently
# rewrite the thing under test.
verify/baseline/
dist/
.astro/
node_modules/
package-lock.json
