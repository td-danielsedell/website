---
/**
 * Font Awesome on the two index pages: NOT deferred, deliberately. The hero
 * expertise reel is above the fold and deferring pops the icons in after first
 * paint. Traps checklist item 7 — do not "fix" this by adding defer.
 */
---
    <!-- Font Awesome Kit 863e7191f5 (SVG+JS). Blocking on purpose: the hero
         expertise reel is above the fold, and deferring pops the icons in
         after first paint. -->
    <script src="https://kit.fontawesome.com/863e7191f5.js" crossorigin="anonymous" is:inline></script>
