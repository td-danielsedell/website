---
/** The area-band.css/td-test.css shared-layout comment, English wording. See
 * TdLinearProductNoteSv for why this is a component rather than an inline
 * comment. */
---

    {/*The product page layout and its components. Exactly the sheet td_test.html
        uses - the two large product pages share a layout, so a change in
        css/td-test.css lands on both.*/}
