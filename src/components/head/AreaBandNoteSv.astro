---
/** The area-band.css load-order comment, Swedish wording. Kept as its own
 * component (rather than a bare comment in a ternary) because Astro drops a
 * top-level comment written directly inside slot content or a Fragment —
 * comments only survive inside a real component's own template. */
---

    <!-- Affärsområden-bandet från startsidan; Fördelar använder samma band.
         Måste ligga efter style.css, se kommentaren överst i arket. -->
