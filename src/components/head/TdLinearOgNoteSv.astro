---
/** og:image PLACEHOLDER note, Swedish wording. Kept as its own component
 * because Astro drops a top-level comment written directly inside slot
 * content or a Fragment — comments only survive inside a real component's
 * own template. td_linear has no artwork of its own yet, so the link
 * preview borrows TD Test's; this comment documents that until
 * images/og/td_linear.jpg exists. */
---
    <!--PLACEHOLDER: TD Linear har ingen egen bild än, så länkförhandsvisningen
        lånar TD Tests. Byt till images/og/td_linear.jpg när artworken finns.-->
